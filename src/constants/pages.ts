import { PageI } from "../interfaces/page.interface";
import { RouteNames } from "./routeNames";

export const pages : PageI[] = [{
    pageName: 'favorite',
    pageLink : `${RouteNames.FAVOURITE}`
}];
